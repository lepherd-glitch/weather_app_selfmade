# OpenWeatherMap API 키
#
# 발급: https://home.openweathermap.org/api_keys (이메일 인증 필수)
# 이 값을 복사해 .env.local 파일을 만들고 실제 키를 채워 넣으세요.
#
# 주의: NEXT_PUBLIC_ 접두사를 붙이면 브라우저 번들에 키가 노출됩니다.
#       이 값은 서버(Server Component / Route Handler)에서만 사용합니다.
OPENWEATHER_API_KEY=your_api_key_here
